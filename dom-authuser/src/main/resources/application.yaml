server:
  port: 8086
spring:
  application:
    name: dom-authuser-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/dom-authuser
    username: postgres
    password: 205015
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka